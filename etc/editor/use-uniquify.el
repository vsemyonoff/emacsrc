;;; use-uniquify.el ---  buffer name unification. -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(use-package uniquify :ensure nil
  :config (setq uniquify-buffer-name-style 'forward))


(provide 'use-uniquify)
;;; set-uniquify.el ends here
