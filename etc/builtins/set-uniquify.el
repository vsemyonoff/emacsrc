;;; set-uniquify.el ---  buffer name unification. -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(require 'uniquify)
(setq uniquify-buffer-name-style 'forward)

(provide 'set-uniquify)
;;; set-uniquify.el ends here
